<%= turbo_stream.append("comment-thread-#{@comment.id}",
                        partial: 'facility/facilities/reply',
                        locals: { reply: @reply }) %>

<%= turbo_stream.update @comment do %>
  <%= render 'facility/comments/comment', comment: @comment %>
<% end %>
