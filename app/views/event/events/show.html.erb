<div class="container m-5">
  <div class="card text-center mx-auto">
    <div class="card-body">
      <h3 class="card-title">テニス　対戦相手募集！！</h3>
      <h5>開催日　９月１８日</h5>
      <table class="table table-bordered m-auto">
        <tr class="table-success">
          <td>開始時間</td>
          <td>１３：００</td>
        </tr>
        <tr class="table-primary">
          <td>終了時間</td>
          <td>１７：００</td>
        </tr>
        <tr class="table-light">
          <td>募集人数</td>
          <td>１０人</td>
        </tr>
      </table>
      <p class="card-text m-0 text-start ms-1">
        テニス　初心者歓迎　〇〇大学
      </p>
      <h5 class='text-start m-3'>詳細</h5>
      <p class='text-start m-3'>
        〇〇大学テニスサークルです！<br>
        経験年数１〜２年程度のメンバーが１０人程度参加予定です。<br><br>
        ・ラケット等の道具は持参<br>
        ・参加費　一人あたり３００円<br>
        ・開催場所　〇〇庭球場<br><br>
        こちらの条件で可能なサークル等ございましたら<br>ぜひ気軽にご参加ください！<br>
        連絡先　hogehoge@example.com
      </p>
      <% if current_user&.own_event?(@event) %>
        <%= link_to  "編集", edit_event_path(@event), class: 'mt-3 btn btn-success' %>
        <%= link_to  "イベントを中止する", "#", class: 'mt-3 btn btn-danger' %>
      <% elsif logged_in? %>
        <%= render 'event/events/participation_button', event: @event %>
      <% else %>
        <p class="mb-0">ユーザー登録するとイベント参加が可能になります。</p>
      <% end %>
      <% if @event.recruitment? %>
        <p>募集中</p>
      <% else %>
        <p>募集は終了しました</p>
      <% end %>
    </div>
  </div>

<!-- コメント -->
  <div class="mt-5">
    <%= turbo_frame_tag 'comments' do %>
      <%= render partial: 'event/comments/comment_thread', collection: @comments, as: 'comment' %>
    <% end %>
    <%= render 'event/comments/comment_form', { event: @event, comment: @comment, url: event_comments_path(@event) } %>
  </div>

</div>

